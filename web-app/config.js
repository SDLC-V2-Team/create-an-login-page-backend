// Runtime configuration for the Web Application.
// Override API_BASE_URL at deploy time (e.g. via an entrypoint that rewrites this file).
window.APP_CONFIG = {
  API_BASE_URL: 'http://localhost:4000'
};
